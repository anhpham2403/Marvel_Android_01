<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >
    <data>
        <variable
            name="viewModel"
            type="com.framgia.moviedb.data.model.Actor"
            />
        <variable
            name="listener"
            type="com.framgia.moviedb.screen.detail.ActorAdapter.OnItemClickListener"/>
    </data>
    <android.support.v7.widget.CardView
        android:id="@+id/card_view_actor"
        android:layout_width="@dimen/image_width"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/card_movie_margin"
        app:cardCornerRadius="@dimen/card_movie_radius"
        android:onClick="@{()->listener.onItemClickListener(viewModel)}"
        >
        <LinearLayout
            android:layout_width="@dimen/image_width"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            >
            <ImageView
                android:id="@+id/image_actor"
                android:layout_width="@dimen/image_width_100"
                android:layout_height="@dimen/image_height_150"
                android:background="?attr/selectableItemBackgroundBorderless"
                ignore="ContentDescription"
                android:scaleType="fitXY"
                app:imageUrl="@{viewModel.avatarUrl}"
                />
            <TextView
                android:id="@+id/text_name_actor"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:paddingTop="@dimen/card_movie_margin"
                android:text="@{viewModel.name}"
                android:textColor="@color/color_black"
                android:textSize="@dimen/text_title"
                android:textStyle="normal"
                />
        </LinearLayout>
    </android.support.v7.widget.CardView>
</layout>